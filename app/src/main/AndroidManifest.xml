<manifest
    package="com.example.ivan.konverzijavaluta"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".main.KonverzijaValutaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".main.MainStartingActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".database.KonverzijaProvider"
            android:authorities="@string/provider_authority"
            android:exported="false"
            android:multiprocess="true" />

        <service android:name=".service.DownloadIntentService" />

    </application>

</manifest>
